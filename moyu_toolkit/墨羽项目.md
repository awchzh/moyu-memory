# 墨羽（MOYU）项目知识库

> 这个文件记录 MOYU 项目的核心信息：定位、流程、规则、历史。每次讨论相关话题前先读此文件，有新共识后更新此文件。用完即走。

---

## 一、项目定位

MOYU 是一个**轻量、框架无关的 AI Agent 记忆工具包**。

- 目标用户：个人开发者、小团队
- 理念：做对一件事做到极致，不堆砌功能
- 边界：不破坏其他模块，核心代码保持框架无关
- 选型：不走平台绑定路线（不学大公司做生态锁定），即插即走

## 二、发布流程

### GitHub Release

1. 修改 `updater.py` 中的 `VERSION` 和 `_CHECKSUMS`
2. 先算好本地的 `git archive` zip SHA256，再 commit（避免先 commit 再改 checksum 需要 amend）
   ```bash
   git archive --format=zip --prefix=moyu_toolkit/ -o /tmp/moyu-toolkit-X.X.X.zip vX.X.X:moyu_toolkit/
   # 算 SHA256
   ```
3. commit + tag + push
   ```bash
   git add -A
   git commit -m "vX.X.X — 变更说明（英文）"
   git tag -a vX.X.X -m "vX.X.X"
   git push origin main --tags
   ```
4. GitHub Release 页面创建 Release：
   - 标题：`vX.X.X — 一句话描述`
   - 描述：英文，只列实际变更，不画饼
   - 链接：`https://github.com/awchzh/moyu-memory/releases/new?tag=vX.X.X`

### SkillHub 上传

1. 从 tag 打包，只打 `moyu_toolkit/` 目录（不要整个仓库）
   ```bash
   git archive --format=zip --prefix=moyu_toolkit/ -o /tmp/moyu-toolkit-X.X.X.zip vX.X.X:moyu_toolkit/
   ```
2. 确认 zip 包含 `moyu_toolkit/SKILL.md`
3. 变更说明：中文，和 GitHub 对应的内容
4. 上传到 SkillHub，版本号与 GitHub 一致

### 注意事项

- GitHub 描述用英文，SkillHub 用中文
- 两个平台的版本号保持一致
- updater.py 的 `_CHECKSUMS` 需要在 Release 前就写入正确的 SHA256
- ZIP 只含 `moyu_toolkit/`，不含 LICENSE/README 等根目录文件（SkillHub 要求）

## 三、版本历史

| 版本 | 日期 | 内容 |
|------|------|------|
| v1.0 | 2026-05-10 | 首次公开，10 大能力 |
| v1.1 | 2026-05-11 | 自适应 Learner |
| v1.2 | 2026-05-12 | 安全加固 + 英文化 + 双语种子词 |
| v1.3 | 2026-05-12 | Demo 自适应架构 + 统一 CLI |
| v2.0 | 2026-05-13 | 记忆生命周期管理（压缩/遗忘/合并/更新） |
| v2.0.1 | 2026-05-13 | 安全加固（checksum + 去 exec），功能不变 |
| v2.0.2 | 2026-05-13 | 功能完善：用户画像、知识图谱 fallback、法医分析增强 |
| v2.0.3 | 2026-05-13 | 新增：自我反思（真实可用）、工作流知识库检索（`moyu kb`） |
| v2.0.4 | 2026-05-13 | 安全体系完善：自动备份与恢复（每日3天）、`moyu audit`、防御链可视化、初始化引导。更新项目定位 |

## 四、代码架构

```
moyu_toolkit/
├── agent_memory.py      # TEMPR 多策略检索（语义 + BM25 + 时间）
├── active_context.py    # 工作记忆管理
├── knowledge_graph.py   # 轻量知识图谱
├── learner.py           # 从纠正中学习 + 用户画像
├── security.py          # 记忆自我保护（密码验证）
├── defense_toolkit/
│   └── integrity_checker.py  # 完整性校验 + 自动恢复 + 法医分析
├── context_manager.py   # 上下文自适应压缩
├── forgetting_curve.py  # 遗忘曲线
├── memory_merge.py      # 记忆合并
├── session_bridge.py    # 跨会话桥接（内部工具，非发布能力）
├── self_reflection.py   # 自我反思（矛盾检测+关联发现，V2.0.3）
├── updater.py           # 自动更新（含 SHA256 校验）
├── moyu.py              # 统一 CLI 入口
├── moyu_demo.py         # Demo 模式
├── moyu_wake.py         # 唤醒例程
├── config.yaml          # 配置
├── SKILL.md             # SkillHub 描述文档
├── requirements.txt     # 依赖
└── 墨羽项目.md          # 项目知识库（本文件）
```

## 五、关键决策记录

| 日期 | 决策 | 原因 |
|------|------|------|
| 2026-05-12 | GitHub 全英文，SkillHub 全中文 | 用户明确要求，分清楚 |
| 2026-05-12 | 自适应 Demo 架构 | 每个模块写 .demo()，moyu_demo.py 做发现引擎，加新功能不改 demo 脚本 |
| 2026-05-13 | v2.0.1 只修两个安全项，不加多余改动 | 用户纠正：不要擅自加入本次不该改的内容 |
| 2026-05-13 | 核心代码保持框架无关，MCP Server 作为增量 | 不牺牲轻量路线 |
| 2026-05-13 | 跨会话桥接从能力列表撤下，不向用户推广 | 未解决的功能不该标上去，用户买的是成品不是路线图 |
| 2026-05-13 | mobai_framework.md 场景B增加"讨论/执行"区分步骤 | 没有明确"去做"指令时擅自动手，多次被纠正。先确认再动手。 |
| 2026-05-13 | 原则确立：标上去的功能必须真实可用，否则不标 | v2.0 跨会话桥接标了但不好用，用户画像标了但后端空缺——不要再出现货不对板 |

## 六、更新方式

墨白每次醒来讨论 MOYU 相关话题前：
1. 先读此文件，回顾上下文
2. 讨论中有新共识，立即更新此文件
3. 发布操作参照「发布流程」章节，不靠记忆
