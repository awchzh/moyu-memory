---
|name: moyu
description: 墨羽-自适应记忆工具包。TEMPR 多策略检索、工作记忆、知识图谱、用户画像、从纠正中学习、自我反思、完整性校验、自动恢复、法医分析、记忆自我保护、上下文自适应压缩、遗忘曲线、记忆合并、跨会话桥接、自动更新等 16 大能力。
tags: [ai-agent, memory, python, agent-memory, llm, rag, knowledge-graph, security, 记忆, 工具包, AI Agent, 向量检索, 知识图谱, 检索增强, 自适应压缩, 记忆生命周期]
---

# 墨羽-自适应记忆工具包

> 给你的 AI Agent 装上 16 大记忆能力，跨会话真正记得你是谁。零平台绑定，开箱即用。

GitHub 仓库：https://github.com/awchzh/moyu-memory

## 16 大能力 — 按领域分组

### 📦 记忆层 — 存储与检索

| # | 能力 | 说明 |
|---|------|------|
| 1️⃣ | **TEMPR 多策略检索** | 语义 + BM25 关键词 + 时间加权，三重保障 |
| 2️⃣ | **工作记忆** | 独立文件存储，上下文压缩也不丢 |
| 3️⃣ | **轻量知识图谱** | 自动提取实体关系（JSON，零数据库） |
| 4️⃣ | **用户画像** | 自动从对话中提取用户偏好和事实 |
| 5️⃣ | **防重复** | SHA256 哈希去重，不会重复存储 |

### 🧠 学习层 — 从互动中进化

| # | 能力 | 说明 |
|---|------|------|
| 6️⃣ | **从纠正中学习** | 识别"不对/不要/记住"信号，3次晋升为永久规则，同类错误不再犯 |
| 7️⃣ | **自我反思** | 醒来时自动分析旧记忆，发现关联与矛盾 |

### 🛡️ 防御层 — 保护与验证

| # | 能力 | 说明 |
|---|------|------|
| 8️⃣ | **完整性校验** | 检测记忆文件篡改 |
| 9️⃣ | **自动恢复** | 检测到篡改后自动从备份恢复 |
| 🔟 | **法医分析** | 分析篡改来源——指令覆盖、提示词注入等 |
| 1️⃣1️⃣ | **记忆自我保护** | 第一道防火墙——关键操作前密码验证，操作到达记忆文件之前就阻止，防误删防篡改 |

### 🌿 生命周期层 — 让记忆会呼吸（V2.0）

| # | 能力 | 说明 |
|---|------|------|
| 1️⃣2️⃣ | **上下文自适应压缩** | 实时检测上下文占用率。80% 预警通知用户，90% 自动静默压缩——降级低优先级项、截断长记忆、节省 token。支持手动触发（`moyu compress --now`）。 |
| 1️⃣3️⃣ | **遗忘曲线** | 14 天未访问的记忆自动降级——但**只在上下文有压力时才触发**（低频率用户一条不动）。降级记忆仍可搜索，重新访问即可恢复。 |
| 1️⃣4️⃣ | **记忆合并** | 检测关键词重叠的相关记忆，自动合并为一条综合摘要。原始细节存入可展开字段，不丢失任何信息。 |
| 1️⃣5️⃣ | **跨会话桥接** | 自动在会话之间传承主题和关键信息。新窗口开口说"继续上次"——上下文已经就绪。不提旧事——零开销。 |
| 1️⃣6️⃣ | **自动更新** | 自动检测 GitHub 新版本，一键升级工具包——保留 memory_data 和用户配置。`moyu update` 检查，`moyu update now` 更新。 |

## 快速开始

```bash
pip install numpy requests pyyaml
```

把 `moyu_toolkit/` 文件夹复制到你的项目中：

```bash
cd moyu_toolkit
python3 agent_memory.py search "我们聊过什么"
```

**一条命令统领全局：**

```bash
cd moyu_toolkit && python3 moyu.py --help
```

搜索、统计、压缩、遗忘、合并、更新——一个命令搞定。

## 对比

| 对比项 | 平台自带（Hermes/OpenClaw） | **墨羽** |
|--------|---------------------------|---------|
| 存储方式 | 纯文本 | 向量索引（1536维语义） |
| 检索方式 | 全文读取 | **TEMPR 三重策略** |
| 工作记忆 | ❌ 无 | ✅ 抗压缩 |
| 知识图谱 | ❌ 无 | ✅ JSON 文件 |
| 自我反思 | ❌ 无 | ✅ 自动 |
| 用户画像 | ❌ 手动 | ✅ 自动 |
| 学习纠正 | ❌ 无 | ✅ 自动 |
| 完整性校验 | ❌ 无 | ✅ manifest + SHA256 |
| 自动恢复 | ❌ 无 | ✅ 从备份恢复 |
| 法医分析 | ❌ 无 | ✅ 攻击来源分析 |
| 记忆自我保护 | ❌ 无 | ✅ 事前验证，操作前拦截 |
| 上下文自适应压缩 | ❌ 无 | ✅ 80%预警 / 90%自动压 / 手动触发 |
| 遗忘曲线 | ❌ 无 | ✅ 压力驱动生命周期 |
| 记忆合并 | ❌ 无 | ✅ 关键词重叠自动合并 |
| 跨会话桥接 | ❌ 无 | ✅ 自动传承上下文 |
| 自动更新 | ❌ 无 | ✅ GitHub 一键更新，保留数据 |
| API 切换 | 固定 | ✅ 随意切换 |
| 平台绑定 | 绑定平台 | ✅ 零绑定 |

## 适用平台

Hermes、OpenClaw、LangChain、AutoGen，或任何基于 Python 的 AI Agent。

## 许可证

MIT
